[flake8]
max-line-length = 88
extend-ignore = E203, W503

[tool:pytest]
testpaths = tests tests_extended
markers =
    integration: marks tests that hit live network (deselect with -m "not integration")
